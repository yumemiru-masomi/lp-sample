import { Button } from ".";

export const Story = () => (
  <div style={{ marginBottom: 80 }}>
    <Button>今すぐ申し込む</Button>
  </div>
);

export default {
  title: "src/Button",
  component: Button,
  tags: ["autodocs"],
};
